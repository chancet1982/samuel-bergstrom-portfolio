const typography = {
  minSize: 1,
  maxSize: 1.24,
  size: 1.24, // TODO: maybe the base size needs to be updated based on resolution as well. size in rem for typography (remove this once implemented as part of the title as well)
  lh: 1.6, // base line-height
  maxHMod: 0.32,
  minHMod: 0.16,
  maxBMod: 0.16,
  minBMod: 0.08,
  inc: 0.1, // line height modifier
  ftMin: 560,
  ftMax: 1440,
  headlineFont: `'Mona Sans', sans-serif`,
  bodyFont: `'Mona Sans', sans-serif`,
};

export default typography;
